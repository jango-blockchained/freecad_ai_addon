[pytest]
# Limit test discovery to our repo and avoid vendor/embedded dirs
addopts = -v --tb=short
python_files = test_*.py
# Prefer our top-level tests and the package tests
testpaths =
    .
    tests
norecursedirs =
    venv
    .*
    build
    dist
    squashfs-root
    __pycache__
